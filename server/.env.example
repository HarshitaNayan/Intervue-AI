# GEMINI_API_KEY is no longer used by the server — InterVue AI is BYOK
# (Bring Your Own Key). Each visitor enters their own free Gemini API key
# in the browser; it's sent per-request from the client and never stored
# here. Get a free key at https://aistudio.google.com/apikey if you want
# one for your own local testing in the browser prompt.
PORT=4000
