// Bumped on every delivered zip so it's possible to tell at a glance,
// from a screenshot, whether a bug report is against the build that
// was actually just fixed or a stale/cached one.
export const BUILD_TAG = 'build-2026-07-24-f';
